# Project conventions

## README style

Do not put code samples in `README.md`. README describes *what* the project is and *why* it exists; runnable examples belong in `samples/`. When new functionality needs an example, add or extend a file under `samples/` and reference it by name in the README — never inline a ```python block.
